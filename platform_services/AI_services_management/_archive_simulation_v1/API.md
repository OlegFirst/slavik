# simulation - API Documentation

> Автоматически сгенерированная документация API endpoints

**Всего endpoints:** 233
**Ресурсов:** 52
**Последнее обновление:** 2025-10-07

---

## 📋 Содержание

- [](#)
- [ai-generate](#ai-generate)
- [ai-generate-advanced](#ai-generate-advanced)
- [ai-scenarios](#ai-scenarios)
- [analyze](#analyze)
- [api](#api)
- [bia](#bia)
- [categories](#categories)
- [compute](#compute)
- [csv](#csv)
- [dashboard](#dashboard)
- [engines](#engines)
- [generate](#generate)
- [health](#health)
- [hub](#hub)
- [json](#json)
- [learn-from-exercise](#learn-from-exercise)
- [learning](#learning)
- [library](#library)
- [liveness](#liveness)
- [login](#login)
- [logout](#logout)
- [me](#me)
- [monte-carlo](#monte-carlo)
- [odoo](#odoo)
- [optimize](#optimize)
- [organization](#organization)
- [ping](#ping)
- [predictions](#predictions)
- [queue-theory](#queue-theory)
- [readiness](#readiness)
- [recommend](#recommend)
- [refresh](#refresh)
- [register](#register)
- [salesforce](#salesforce)
- [scenarios](#scenarios)
- [series](#series)
- [simulate](#simulate)
- [simulations](#simulations)
- [statistics](#statistics)
- [status](#status)
- [template](#template)
- [test](#test)
- [twin](#twin)
- [types](#types)
- [{bia_id}](#{bia_id})
- [{exercise_id}](#{exercise_id})
- [{org_id}](#{org_id})
- [{prediction_id}](#{prediction_id})
- [{scenario_id}](#{scenario_id})
- [{sim_id}](#{sim_id})
- [{twin_id}](#{twin_id})

---

## 

### `GET` /

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `simple_app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `bia.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `predictions.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `simulations.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `organizations.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /

**Файл:** `exercises.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /

**Файл:** `bia.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /

**Файл:** `predictions.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /

**Файл:** `simulations.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /

**Файл:** `organizations.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /

**Файл:** `exercises.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## ai-generate

### `POST` /ai-generate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/ai-generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## ai-generate-advanced

### `POST` /ai-generate-advanced

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/ai-generate-advanced" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## ai-scenarios

### `POST` /ai-scenarios/generate/{twin_id}

**Файл:** `integrations.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/ai-scenarios/generate/{twin_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## analyze

### `POST` /analyze

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/analyze" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /analyze

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/analyze" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /analyze

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/analyze" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /analyze

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/analyze" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## api

### `POST` /api/alerts

**Файл:** `thehive_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/alerts" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/alerts/{alert_id}/promote

**Файл:** `thehive_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/alerts/{alert_id}/promote" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/batch/simulations

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/batch/simulations" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/cases

**Файл:** `thehive_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/cases" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/cases

**Файл:** `thehive_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/cases" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/cases/create

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/cases/create" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/cases/search

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/cases/search" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/cases/{case_id}

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/cases/{case_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/cases/{case_id}/observables

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/cases/{case_id}/observables" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/cases/{case_id}/tasks

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/cases/{case_id}/tasks" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `PUT` /api/cases/{case_id}/update

**Файл:** `app.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/api/cases/{case_id}/update" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/exercises

**Файл:** `sim_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/exercises" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/exercises

**Файл:** `sim_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/exercises" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/exercises/{exercise_id}/complete

**Файл:** `sim_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/exercises/{exercise_id}/complete" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/exercises/{exercise_id}/simulations

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/exercises/{exercise_id}/simulations" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/exercises/{exercise_id}/start

**Файл:** `sim_adapter.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/exercises/{exercise_id}/start" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/metrics

**Файл:** `thehive_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/metrics" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/metrics

**Файл:** `sim_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/metrics" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/metrics/{tenant_id}

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/metrics/{tenant_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/reports/{simulation_id}

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/reports/{simulation_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/scenarios

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/scenarios" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/scenarios

**Файл:** `sim_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/scenarios" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/scenarios/custom

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/scenarios/custom" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/scenarios/validate

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/scenarios/validate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/scenarios/{scenario_name}

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/scenarios/{scenario_name}" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/simulations/start

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/simulations/start" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/simulations/{simulation_id}/results

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/simulations/{simulation_id}/results" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/simulations/{simulation_id}/status

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/simulations/{simulation_id}/status" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/simulations/{simulation_id}/stop

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/simulations/{simulation_id}/stop" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/statistics/{tenant_id}

**Файл:** `app.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/statistics/{tenant_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/templates

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/templates" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/configs

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/configs" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/thehive/configs

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/configs" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/thehive/mock/alerts

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/alerts" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/mock/cases

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/cases" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/mock/configs

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/configs" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/mock/metrics

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/metrics" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/mock/observables

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/observables" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/thehive/mock/setup-demo-config

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/mock/setup-demo-config" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/thehive/mock/tasks

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/tasks" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/mock/templates

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/mock/templates" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/thehive/{config_id}/alerts

**Файл:** `main.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/{config_id}/alerts" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/thehive/{config_id}/alerts

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/{config_id}/alerts" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/thehive/{config_id}/alerts/{alert_id}/promote

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/{config_id}/alerts/{alert_id}/promote" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/thehive/{config_id}/bcm/incident

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/{config_id}/bcm/incident" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/thehive/{config_id}/cases

**Файл:** `main.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/{config_id}/cases" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/thehive/{config_id}/cases

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/{config_id}/cases" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/thehive/{config_id}/cases/{case_id}

**Файл:** `main.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/thehive/{config_id}/cases/{case_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PATCH` /api/thehive/{config_id}/cases/{case_id}

**Файл:** `main.py`

**Описание:**  
Операция PATCH

**Пример запроса:**

```bash
curl -X PATCH \
  "http://localhost:8000/api/thehive/{config_id}/cases/{case_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/thehive/{config_id}/cases/{case_id}/tasks

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/thehive/{config_id}/cases/{case_id}/tasks" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/v1/case/{case_id}

**Файл:** `bridge_service.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/case/{case_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PUT` /api/v1/case/{case_id}

**Файл:** `bridge_service.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/api/v1/case/{case_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/v1/case/{case_id}/sync

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/case/{case_id}/sync" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/v1/cases

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/cases" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/v1/exercise/create-case

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/exercise/create-case" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/v1/exercises

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/exercises" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/v1/exercises/create

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/exercises/create" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/v1/exercises/{exercise_id}/complete

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/exercises/{exercise_id}/complete" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/v1/exercises/{exercise_id}/inject

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/exercises/{exercise_id}/inject" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /api/v1/exercises/{exercise_id}/start

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/exercises/{exercise_id}/start" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/v1/exercises/{exercise_id}/status

**Файл:** `bridge_service.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/exercises/{exercise_id}/status" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/v1/incident/create-case

**Файл:** `bridge_service.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/v1/incident/create-case" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /api/v1/metrics

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/metrics" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/v1/metrics

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/metrics" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/v1/scenarios/status

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/scenarios/status" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /api/v1/scenarios/status

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/api/v1/scenarios/status" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /api/webhooks/thehive

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/api/webhooks/thehive" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## bia

### `POST` /bia/analyze/{twin_id}

**Файл:** `integrations.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/bia/analyze/{twin_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /bia/status/{twin_id}

**Файл:** `integrations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/bia/status/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

## categories

### `GET` /categories/available

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/categories/available" \
  -H "Authorization: Bearer <token>"
```

---

## compute

### `POST` /compute

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/compute" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /compute

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/compute" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## csv

### `POST` /csv

**Файл:** `import_data.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/csv" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## dashboard

### `GET` /dashboard/{twin_id}

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/dashboard/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

## engines

### `GET` /engines

**Файл:** `simulation_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/engines" \
  -H "Authorization: Bearer <token>"
```

---

## generate

### `POST` /generate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /generate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /generate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /generate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## health

### `GET` /health

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `thehive_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `simple_app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `app.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `bridge_service.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `sim_adapter.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health

**Файл:** `integrations.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /health

**Файл:** `metrics.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/health" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /health/{twin_id}

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /health/{twin_id}/latest

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/health/{twin_id}/latest" \
  -H "Authorization: Bearer <token>"
```

---

## hub

### `GET` /hub/catalog

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/hub/catalog" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /hub/catalog

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/hub/catalog" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /hub/catalog

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/hub/catalog" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /hub/catalog

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/hub/catalog" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /hub/submit

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/hub/submit" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /hub/submit

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/hub/submit" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /hub/submit

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/hub/submit" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /hub/submit

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/hub/submit" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## json

### `POST` /json

**Файл:** `import_data.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/json" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## learn-from-exercise

### `POST` /learn-from-exercise

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/learn-from-exercise" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## learning

### `GET` /learning/dashboard

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/learning/dashboard" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /learning/dashboard

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/learning/dashboard" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /learning/exercise-result

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/learning/exercise-result" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /learning/exercise-result

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/learning/exercise-result" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /learning/scenario/{scenario_id}/insights

**Файл:** `main.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/learning/scenario/{scenario_id}/insights" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /learning/scenario/{scenario_id}/insights

**Файл:** `main.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/learning/scenario/{scenario_id}/insights" \
  -H "Authorization: Bearer <token>"
```

---

## library

### `GET` /library

**Файл:** `scenario_library_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/library" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /library/complexity-levels

**Файл:** `scenario_library_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/library/complexity-levels" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /library/stats

**Файл:** `scenario_library_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/library/stats" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /library/threat-types

**Файл:** `scenario_library_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/library/threat-types" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /library/{scenario_id}

**Файл:** `scenario_library_router.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/library/{scenario_id}" \
  -H "Authorization: Bearer <token>"
```

---

## liveness

### `GET` /liveness

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/liveness" \
  -H "Authorization: Bearer <token>"
```

---

## login

### `POST` /login

**Файл:** `auth.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/login" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## logout

### `POST` /logout

**Файл:** `auth.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/logout" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## me

### `GET` /me

**Файл:** `auth.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/me" \
  -H "Authorization: Bearer <token>"
```

---

## monte-carlo

### `POST` /monte-carlo

**Файл:** `predictions.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/monte-carlo" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## odoo

### `GET` /odoo/organization/{odoo_id}

**Файл:** `bridges.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/odoo/organization/{odoo_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /odoo/push

**Файл:** `bridges.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/odoo/push" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /odoo/status

**Файл:** `bridges.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/odoo/status" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /odoo/sync

**Файл:** `bridges.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/odoo/sync" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## optimize

### `POST` /optimize

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /optimize

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /optimize

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /optimize

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /optimize/single-process

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize/single-process" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /optimize/single-process

**Файл:** `app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/optimize/single-process" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## organization

### `GET` /organization/{org_id}/summary

**Файл:** `predictions.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/organization/{org_id}/summary" \
  -H "Authorization: Bearer <token>"
```

---

## ping

### `GET` /ping

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/ping" \
  -H "Authorization: Bearer <token>"
```

---

## predictions

### `POST` /predictions

**Файл:** `metrics.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/predictions" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /predictions/twin/{twin_id}

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/predictions/twin/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /predictions/{pred_id}

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/predictions/{pred_id}" \
  -H "Authorization: Bearer <token>"
```

---

## queue-theory

### `POST` /queue-theory

**Файл:** `bia.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/queue-theory" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## readiness

### `GET` /readiness

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/readiness" \
  -H "Authorization: Bearer <token>"
```

---

## recommend

### `POST` /recommend

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/recommend" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /recommend

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/recommend" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /recommend

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/recommend" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /recommend

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/recommend" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## refresh

### `POST` /refresh

**Файл:** `auth.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/refresh" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## register

### `POST` /register

**Файл:** `auth.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/register" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## salesforce

### `GET` /salesforce/status

**Файл:** `bridges.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/salesforce/status" \
  -H "Authorization: Bearer <token>"
```

---

## scenarios

### `GET` /scenarios

**Файл:** `scenario_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/scenarios" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /scenarios

**Файл:** `scenario_router.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/scenarios" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /scenarios/available

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/scenarios/available" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /scenarios/available

**Файл:** `main.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/scenarios/available" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /scenarios/generate

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/scenarios/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /scenarios/generate

**Файл:** `main.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/scenarios/generate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `DELETE` /scenarios/{scenario_id}

**Файл:** `scenario_router.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/scenarios/{scenario_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /scenarios/{scenario_id}

**Файл:** `scenario_router.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/scenarios/{scenario_id}" \
  -H "Authorization: Bearer <token>"
```

---

## series

### `POST` /series

**Файл:** `metrics.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/series" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /series/{twin_id}

**Файл:** `metrics.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/series/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

## simulate

### `POST` /simulate

**Файл:** `simple_app.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/simulate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## simulations

### `GET` /simulations

**Файл:** `simulation_router.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/simulations" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /simulations

**Файл:** `simulation_router.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/simulations" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `DELETE` /simulations/{sim_id}

**Файл:** `simulation_router.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/simulations/{sim_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /simulations/{sim_id}

**Файл:** `simulation_router.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/simulations/{sim_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /simulations/{sim_id}/results

**Файл:** `execution_router.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/simulations/{sim_id}/results" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /simulations/{sim_id}/run

**Файл:** `execution_router.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/simulations/{sim_id}/run" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /simulations/{sim_id}/status

**Файл:** `execution_router.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/simulations/{sim_id}/status" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /simulations/{sim_id}/stop

**Файл:** `execution_router.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/simulations/{sim_id}/stop" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## statistics

### `GET` /statistics

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/statistics" \
  -H "Authorization: Bearer <token>"
```

---

## status

### `GET` /status

**Файл:** `health.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/status" \
  -H "Authorization: Bearer <token>"
```

---

## template

### `GET` /template/csv

**Файл:** `import_data.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/template/csv" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /template/json

**Файл:** `import_data.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/template/json" \
  -H "Authorization: Bearer <token>"
```

---

## test

### `POST` /test/simulate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/test/simulate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /test/simulate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/test/simulate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /test/simulate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/test/simulate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /test/simulate

**Файл:** `scenarios.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/test/simulate" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## twin

### `GET` /twin/{twin_id}

**Файл:** `organizations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/twin/{twin_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /twin/{twin_id}/latest

**Файл:** `simulations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/twin/{twin_id}/latest" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /twin/{twin_id}/summary

**Файл:** `simulations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/twin/{twin_id}/summary" \
  -H "Authorization: Bearer <token>"
```

---

## types

### `GET` /types/available

**Файл:** `bia.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/types/available" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /types/available

**Файл:** `scenarios.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/types/available" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /types/available

**Файл:** `predictions.py`

**Описание:**  
Получить список

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/types/available" \
  -H "Authorization: Bearer <token>"
```

---

## {bia_id}

### `DELETE` /{bia_id}

**Файл:** `bia.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/{bia_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{bia_id}

**Файл:** `bia.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{bia_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `POST` /{bia_id}/execute

**Файл:** `bia.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/{bia_id}/execute" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## {exercise_id}

### `DELETE` /{exercise_id}

**Файл:** `exercises.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/{exercise_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{exercise_id}

**Файл:** `exercises.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{exercise_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PUT` /{exercise_id}

**Файл:** `exercises.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/{exercise_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /{exercise_id}/complete

**Файл:** `exercises.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/{exercise_id}/complete" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /{exercise_id}/execute

**Файл:** `exercises.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/{exercise_id}/execute" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /{exercise_id}/start

**Файл:** `exercises.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/{exercise_id}/start" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## {org_id}

### `DELETE` /{org_id}

**Файл:** `organizations.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/{org_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{org_id}

**Файл:** `organizations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{org_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PUT` /{org_id}

**Файл:** `organizations.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/{org_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `GET` /{org_id}/data-sources

**Файл:** `organizations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{org_id}/data-sources" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{org_id}/insights

**Файл:** `organizations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{org_id}/insights" \
  -H "Authorization: Bearer <token>"
```

---

## {prediction_id}

### `DELETE` /{prediction_id}

**Файл:** `predictions.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/{prediction_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{prediction_id}

**Файл:** `predictions.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{prediction_id}" \
  -H "Authorization: Bearer <token>"
```

---

## {scenario_id}

### `DELETE` /{scenario_id}

**Файл:** `scenarios.py`

**Описание:**  
Удалить ресурс

**Пример запроса:**

```bash
curl -X DELETE \
  "http://localhost:8000/{scenario_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{scenario_id}

**Файл:** `scenarios.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{scenario_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PUT` /{scenario_id}

**Файл:** `scenarios.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/{scenario_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## {sim_id}

### `GET` /{sim_id}

**Файл:** `simulations.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{sim_id}" \
  -H "Authorization: Bearer <token>"
```

---

### `PUT` /{sim_id}

**Файл:** `simulations.py`

**Описание:**  
Обновить ресурс

**Пример запроса:**

```bash
curl -X PUT \
  "http://localhost:8000/{sim_id}" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `POST` /{sim_id}/execute

**Файл:** `simulations.py`

**Описание:**  
Создать новый ресурс

**Пример запроса:**

```bash
curl -X POST \
  "http://localhost:8000/{sim_id}/execute" \
  -H "Authorization: Bearer <token>"
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

## {twin_id}

### `GET` /{twin_id}/health-trend

**Файл:** `visualize.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{twin_id}/health-trend" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{twin_id}/organization-graph

**Файл:** `visualize.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{twin_id}/organization-graph" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{twin_id}/risk-heatmap

**Файл:** `visualize.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{twin_id}/risk-heatmap" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{twin_id}/simulation-flow

**Файл:** `visualize.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{twin_id}/simulation-flow" \
  -H "Authorization: Bearer <token>"
```

---

### `GET` /{twin_id}/simulation-impact

**Файл:** `visualize.py`

**Описание:**  
Получить детали по ID

**Пример запроса:**

```bash
curl -X GET \
  "http://localhost:8000/{twin_id}/simulation-impact" \
  -H "Authorization: Bearer <token>"
```

---


## 🔗 Интеграция

### Authentication
```python
headers = {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
}
```

### Base URL
```
http://localhost:8000  # Development
https://api.example.com  # Production
```

---

**Сгенерировано:** 2025-10-07 05:07
**Инструмент:** `tools/generators/documentation_generator.py`

# 🚨 ТЕКУЩЕЕ СОСТОЯНИЕ ПЛАТФОРМЫ

## ПРОБЛЕМА:
- BCM модули содержат ошибки (tree→list в Odoo 18)
- При автозагрузке Odoo зависает на инициализации
- Платформа не стартует с BCM модулями

## РЕШЕНИЕ:
1. BCM модули временно перемещены в `/temp-bcm-modules/`
2. Платформа должна запуститься без них
3. После старта - тестировать модули по одному

## ИСПРАВЛЕНО:
- `bcm_base/views/bcm_service_config_views.xml` - tree→list
- Создан `start-platform.sh` для правильного запуска

## СЛЕДУЮЩИЕ ШАГИ:
1. Дождаться запуска Odoo без BCM
2. Копировать BCM модули по одному обратно
3. Исправлять ошибки по мере нахождения
4. Только рабочие модули оставлять в addons
# Analytics Agents

This directory contains analytics agents.

## Agent Structure

Each agent should have:
- `agent.json` - Agent manifest file
- `index.ts` or `index.js` - Agent implementation
- `README.md` - Agent documentation (optional)

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
pyyaml==6.0.1

# HTTP client for integrations
httpx==0.25.0

# Prometheus metrics
prometheus-client==0.19.0

# Redis support (for EventBus)
redis==5.0.1

# Async support (asyncio is built-in, no need to install)
# asyncio==3.4.3  # REMOVED - this is wrong

# Additional dependencies
aiofiles==23.2.1

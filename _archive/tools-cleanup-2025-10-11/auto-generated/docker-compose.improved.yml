version: '3.8'
services:
  workflow-engine:
    container_name: bcm-workflow-engine
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/workflow-engine
      dockerfile: Dockerfile.auto
    ports:
    - 9000:9000
    environment:
    - WORKFLOW_API_PORT=${WORKFLOW_API_PORT}
    - DATABASE_URL=${DATABASE_URL}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:9000/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '9000'
      prometheus.path: /metrics
      service.category: core
      service.type: ai-service
  predictive:
    container_name: bcm-predictive
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/predictive
      dockerfile: Dockerfile.auto
    ports:
    - 8031:8031
    environment:
    - DATABASE_URL=${DATABASE_URL}
    - ENABLE_DAILY_DIGESTS=${ENABLE_DAILY_DIGESTS}
    - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
    - SUPABASE_DB_PASSWORD=${SUPABASE_DB_PASSWORD}
    - SUPABASE_URL=${SUPABASE_URL}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8031/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8031'
      prometheus.path: /metrics
      service.category: core
      service.type: application
  collective:
    container_name: bcm-collective
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/collective
      dockerfile: Dockerfile.auto
    ports:
    - 9000:9000
    environment:
    - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    networks:
    - bcm-network
    depends_on: {}
    healthcheck:
      test: curl -f http://localhost:9000/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '9000'
      prometheus.path: /metrics
      service.category: core
      service.type: application
  ai-workflow-optimizer:
    container_name: bcm-ai-workflow-optimizer
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/ai_workflow_optimizer
      dockerfile: Dockerfile
    ports:
    - 8006:8006
    environment:
    - DATABASE_URL=${DATABASE_URL}
    networks:
    - bcm-network
    healthcheck:
      test: curl -f http://localhost:8006/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8006'
      prometheus.path: /metrics
      service.category: core
      service.type: ai-service
  "\u043C\u043E\u0436\u0435\u0442\u043F\u0440\u0438\u0433\u043E\u0434\u0438\u0442\u0441\u044F":
    container_name: "bcm-\u043C\u043E\u0436\u0435\u0442\u043F\u0440\u0438\u0433\u043E\
      \u0434\u0438\u0442\u0441\u044F"
    profiles:
    - dev
    - prod
    - core
    build:
      context: "../intelligent-core/\u043C\u043E\u0436\u0435\u0442\u043F\u0440\u0438\
        \u0433\u043E\u0434\u0438\u0442\u0441\u044F"
      dockerfile: Dockerfile.auto
    ports:
    - 8005:8005
    environment:
    - ODOO_URL=${ODOO_URL}
    - POSTGRES_URL=${POSTGRES_URL}
    - EVENTBUS_URL=${EVENTBUS_URL}
    - CORS_ORIGINS=${CORS_ORIGINS}
    - AI_ORCHESTRATOR_URL=${AI_ORCHESTRATOR_URL}
    - JWT_SECRET_KEY=${JWT_SECRET_KEY}
    - SMTP_SERVER=${SMTP_SERVER}
    - DATABASE_URL=${DATABASE_URL}
    - PORT=${PORT}
    - REDIS_URL=${REDIS_URL}
    - SMTP_USERNAME=${SMTP_USERNAME}
    - SMTP_PASSWORD=${SMTP_PASSWORD}
    - JWT_ALGORITHM=${JWT_ALGORITHM}
    - JWT_EXPIRE_MINUTES=${JWT_EXPIRE_MINUTES}
    - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    - SMTP_PORT=${SMTP_PORT}
    - FROM_EMAIL=${FROM_EMAIL}
    networks:
    - bcm-network
    healthcheck:
      test: curl -f http://localhost:8005/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8005'
      prometheus.path: /metrics
      service.category: core
      service.type: application
  workflow-intelligence:
    container_name: bcm-workflow-intelligence
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/workflow_intelligence
      dockerfile: Dockerfile.auto
    ports:
    - 9002:9002
    environment:
    - PREFIX=${PREFIX}
    - COMP_WORDS=${COMP_WORDS}
    - TIMEOUT_BACKEND_TEST=${TIMEOUT_BACKEND_TEST}
    - path=${path}
    - DATABRICKS_RUNTIME_VERSION=${DATABRICKS_RUNTIME_VERSION}
    - PIP_EXISTS_ACTION=${PIP_EXISTS_ACTION}
    - LDFLAGS=${LDFLAGS}
    - TEMPORAL_TLS_KEY=${TEMPORAL_TLS_KEY}
    - ANDROID_ROOT=${ANDROID_ROOT}
    - SETUPTOOLS_USE_DISTUTILS=${SETUPTOOLS_USE_DISTUTILS}
    - CPPFLAGS=${CPPFLAGS}
    - PRE_BUILT_SETUPTOOLS_SDIST=${PRE_BUILT_SETUPTOOLS_SDIST}
    - USERPROFILE=${USERPROFILE}
    - SETUPTOOLS_ENFORCE_DEPRECATION=${SETUPTOOLS_ENFORCE_DEPRECATION}
    - __PYVENV_LAUNCHER__=${__PYVENV_LAUNCHER__}
    - _PYPROJECT_HOOKS_BUILD_BACKEND=${_PYPROJECT_HOOKS_BUILD_BACKEND}
    - TEMPORAL_NAMESPACE=${TEMPORAL_NAMESPACE}
    - PATHEXT=${PATHEXT}
    - RANLIB=${RANLIB}
    - HOME=${HOME}
    networks:
    - bcm-network
    depends_on:
      ai-foundation:
        condition: service_healthy
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:9002/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '9002'
      prometheus.path: /metrics
      service.category: core
      service.type: ai-service
  ai-foundation:
    container_name: bcm-ai-foundation
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/ai-foundation
      dockerfile: Dockerfile.auto
    ports:
    - 8030:8030
    environment:
    - OPENAI_API_KEY=${OPENAI_API_KEY}
    - QDRANT_URL=${QDRANT_URL}
    - QDRANT_API_KEY=${QDRANT_API_KEY}
    - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    - VOYAGE_API_KEY=${VOYAGE_API_KEY}
    networks:
    - bcm-network
    depends_on:
      ai-foundation:
        condition: service_healthy
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8030/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8030'
      prometheus.path: /metrics
      service.category: core
      service.type: ai-service
  community-intelligence:
    container_name: bcm-community-intelligence
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/community_intelligence
      dockerfile: Dockerfile.auto
    ports:
    - 9000:9000
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:9000/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '9000'
      prometheus.path: /metrics
      service.category: core
      service.type: ai-service
  orchestration:
    container_name: bcm-orchestration
    profiles:
    - dev
    - prod
    - core
    build:
      context: ../intelligent-core/orchestration
      dockerfile: Dockerfile.auto
    ports:
    - 8002:8002
    environment:
    - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    - SUPABASE_KEY=${SUPABASE_KEY}
    - GITHUB_REPO=${GITHUB_REPO}
    - SUPABASE_URL=${SUPABASE_URL}
    networks:
    - bcm-network
    depends_on: {}
    healthcheck:
      test: curl -f http://localhost:8002/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8002'
      prometheus.path: /metrics
      service.category: core
      service.type: application
  "\u0430\u0440\u0445\u0438\u0432":
    container_name: "bcm-\u0430\u0440\u0445\u0438\u0432"
    profiles:
    - dev
    build:
      context: "../infrastructure/\u0430\u0440\u0445\u0438\u0432"
      dockerfile: Dockerfile.auto
    ports:
    - 8001:8001
    environment:
    - POSTGRES_URL=${POSTGRES_URL}
    - CORS_ORIGINS=${CORS_ORIGINS}
    - RABBITMQ_URL=${RABBITMQ_URL}
    - REDIS_URL=${REDIS_URL}
    - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
    - SUPABASE_URL=${SUPABASE_URL}
    networks:
    - bcm-network
    depends_on:
      infrastructure-services:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8001/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8001'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: application
  integration:
    container_name: bcm-integration
    profiles:
    - dev
    build:
      context: ../infrastructure/integration
      dockerfile: Dockerfile.auto
    ports:
    - 8003:8003
    environment:
    - GITHUB_APP_ID=${GITHUB_APP_ID}
    - DATABASE_URL=${DATABASE_URL}
    networks:
    - bcm-network
    depends_on:
      infrastructure-services:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8003/api/v1/process-mining/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8003'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: application
  runtime:
    container_name: bcm-runtime
    profiles:
    - dev
    build:
      context: ../infrastructure/runtime
      dockerfile: Dockerfile.auto
    ports:
    - 8500:8500
    environment:
    - MESSAGE_RETENTION_HOURS=${MESSAGE_RETENTION_HOURS}
    - EVENTBUS_EXCHANGE=${EVENTBUS_EXCHANGE}
    - DATABASE_URL=${DATABASE_URL}
    - UPSTASH_REDIS_URL=${UPSTASH_REDIS_URL}
    - RABBITMQ_URL=${RABBITMQ_URL}
    - REDIS_URL=${REDIS_URL}
    - PORT=${PORT}
    - MAX_CONNECTIONS_PER_USER=${MAX_CONNECTIONS_PER_USER}
    - EVENTBUS_BACKEND=${EVENTBUS_BACKEND}
    networks:
    - bcm-network
    depends_on:
      infrastructure-services:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8500/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8500'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: application
  observability:
    container_name: bcm-observability
    profiles:
    - dev
    build:
      context: ../infrastructure/observability
      dockerfile: Dockerfile.auto
    ports:
    - 8035:8035
    environment:
    - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
    - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
    - TEAMS_WEBHOOK_URL=${TEAMS_WEBHOOK_URL}
    - PAGERDUTY_ENABLED=${PAGERDUTY_ENABLED}
    - RABBITMQ_URL=${RABBITMQ_URL}
    - SMTP_PASSWORD=${SMTP_PASSWORD}
    - SLACK_ENABLED=${SLACK_ENABLED}
    - NOTIFICATION_SERVICE_URL=${NOTIFICATION_SERVICE_URL}
    - SMS_ENABLED=${SMS_ENABLED}
    - SLACK_CHANNEL_ALERTS=${SLACK_CHANNEL_ALERTS}
    - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}
    - SMTP_USER=${SMTP_USER}
    - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
    - COMPLIANCE_OFFICERS_EMAILS=${COMPLIANCE_OFFICERS_EMAILS}
    - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
    - SMTP_HOST=${SMTP_HOST}
    - COMPLIANCE_DATA_DIR=${COMPLIANCE_DATA_DIR}
    - SUPABASE_URL=${SUPABASE_URL}
    - LOG_LEVEL=${LOG_LEVEL}
    - PAGERDUTY_INTEGRATION_KEY=${PAGERDUTY_INTEGRATION_KEY}
    networks:
    - bcm-network
    healthcheck:
      test: curl -f http://localhost:8035/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8035'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: application
  deployment:
    container_name: bcm-deployment
    profiles:
    - dev
    build:
      context: ../infrastructure/deployment
      dockerfile: Dockerfile.auto
    ports:
    - 8002:8002
    environment: []
    networks:
    - bcm-network
    depends_on:
      infrastructure-services:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8002/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8002'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: application
  gateway:
    container_name: bcm-gateway
    profiles:
    - dev
    - prod
    build:
      context: ../infrastructure/gateway
      dockerfile: Dockerfile.auto
    ports:
    - 8500:8500
    environment:
    - ODOO_API_URL=${ODOO_API_URL}
    - JWT_SECRET=${JWT_SECRET}
    - ODOO_DB_URL=${ODOO_DB_URL}
    - POSTGRES_URL=${POSTGRES_URL}
    - MONGODB_URL=${MONGODB_URL}
    - DATABASE_URL=${DATABASE_URL}
    - RABBITMQ_URL=${RABBITMQ_URL}
    - REDIS_URL=${REDIS_URL}
    - ODOO_DB_NAME=${ODOO_DB_NAME}
    - REDIS_PASSWORD=${REDIS_PASSWORD}
    - SUPABASE_KEY=${SUPABASE_KEY}
    - SUPABASE_URL=${SUPABASE_URL}
    networks:
    - bcm-network
    healthcheck:
      test: curl -f http://localhost:8500/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 1G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8500'
      prometheus.path: /metrics
      service.category: infrastructure
      service.type: gateway
  validation-service:
    container_name: bcm-validation-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/validation-service
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment:
    - EMAIL_USERNAME=${EMAIL_USERNAME}
    - EMAIL_PASSWORD=${EMAIL_PASSWORD}
    - EMAIL_PORT=${EMAIL_PORT}
    - EMAIL_HOST=${EMAIL_HOST}
    - EMAIL_USE_TLS=${EMAIL_USE_TLS}
    - EMAIL_FROM=${EMAIL_FROM}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  compliance-service:
    container_name: bcm-compliance-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/compliance-service
      dockerfile: Dockerfile
    ports:
    - 8012:8012
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8012/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8012'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  governance-service:
    container_name: bcm-governance-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/governance-service
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  plans-service:
    container_name: bcm-plans-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/plans_service
      dockerfile: Dockerfile
    ports:
    - 8010:8010
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  planning-service:
    container_name: bcm-planning-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/planning_service
      dockerfile: Dockerfile
    ports:
    - 8010:8010
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  living-docs:
    container_name: bcm-living-docs
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/living-docs
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment:
    - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    networks:
    - bcm-network
    depends_on: {}
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  risk-service:
    container_name: bcm-risk-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/risk-service
      dockerfile: Dockerfile.auto
    ports:
    - 8040:8040
    environment:
    - CORS_ORIGINS=${CORS_ORIGINS}
    - DEBUG=${DEBUG}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8040/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8040'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  simulation:
    container_name: bcm-simulation
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/simulation
      dockerfile: Dockerfile.auto
    ports:
    - 8031:8031
    environment:
    - MAX_CONCURRENT_CASES=${MAX_CONCURRENT_CASES}
    - PREFIX=${PREFIX}
    - TQDM_DISCORD_CHANNEL_ID=${TQDM_DISCORD_CHANNEL_ID}
    - PIP_EXISTS_ACTION=${PIP_EXISTS_ACTION}
    - PGPASSWORD=${PGPASSWORD}
    - REDIS_DB=${REDIS_DB}
    - LDFLAGS=${LDFLAGS}
    - CASE_CREATION_TIMEOUT=${CASE_CREATION_TIMEOUT}
    - Path=${Path}
    - PGDATABASE=${PGDATABASE}
    - THEHIVE_URL=${THEHIVE_URL}
    - PGPASSFILE=${PGPASSFILE}
    - __PYVENV_LAUNCHER__=${__PYVENV_LAUNCHER__}
    - SCENARIO_AI_URL=${SCENARIO_AI_URL}
    - SYNC_CASE_UPDATES=${SYNC_CASE_UPDATES}
    - DEFAULT_TLP=${DEFAULT_TLP}
    - AI_ORCHESTRATOR_URL=${AI_ORCHESTRATOR_URL}
    - RETRY_DELAY=${RETRY_DELAY}
    - PATHEXT=${PATHEXT}
    - PENDULUM_EXTENSIONS=${PENDULUM_EXTENSIONS}
    networks:
    - bcm-network
    healthcheck:
      test: curl -f http://localhost:8031/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8031'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  community-service:
    container_name: bcm-community-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/community-service
      dockerfile: Dockerfile.auto
    ports:
    - 8031:8031
    environment:
    - MARKETPLACE_URL=${MARKETPLACE_URL}
    - CORS_ORIGINS=${CORS_ORIGINS}
    - EVENTBUS_URL=${EVENTBUS_URL}
    - AI_ORCHESTRATOR_URL=${AI_ORCHESTRATOR_URL}
    - VALIDATION_SERVICE_URL=${VALIDATION_SERVICE_URL}
    - DATABASE_URL=${DATABASE_URL}
    - DB_POOL_TIMEOUT=${DB_POOL_TIMEOUT}
    - PORTAL_URL=${PORTAL_URL}
    - PORT=${PORT}
    - DB_MAX_OVERFLOW=${DB_MAX_OVERFLOW}
    - CLIENTS_SERVICE_URL=${CLIENTS_SERVICE_URL}
    - GOVERNANCE_SERVICE_URL=${GOVERNANCE_SERVICE_URL}
    - DB_POOL_SIZE=${DB_POOL_SIZE}
    - DB_ECHO=${DB_ECHO}
    - DEBUG=${DEBUG}
    - LEARNING_SERVICE_URL=${LEARNING_SERVICE_URL}
    networks:
    - bcm-network
    depends_on: {}
    healthcheck:
      test: curl -f http://localhost:8031/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8031'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  response-service:
    container_name: bcm-response-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/response-service
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment:
    - RABBITMQ_PORT=${RABBITMQ_PORT}
    - RABBITMQ_USER=${RABBITMQ_USER}
    - RABBITMQ_EXCHANGE=${RABBITMQ_EXCHANGE}
    - ENVIRONMENT=${ENVIRONMENT}
    - RABBITMQ_HOST=${RABBITMQ_HOST}
    - EVENT_BUS_ENABLED=${EVENT_BUS_ENABLED}
    - RABBITMQ_QUEUE=${RABBITMQ_QUEUE}
    - RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  learning-service:
    container_name: bcm-learning-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/learning-service
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment:
    - DATABASE_URL=${DATABASE_URL}
    - DB_POOL_TIMEOUT=${DB_POOL_TIMEOUT}
    - DB_MAX_OVERFLOW=${DB_MAX_OVERFLOW}
    - PYTHONPATH=${PYTHONPATH}
    - DB_POOL_SIZE=${DB_POOL_SIZE}
    - DB_ECHO=${DB_ECHO}
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  bia-service:
    container_name: bcm-bia-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/bia-service
      dockerfile: Dockerfile
    ports:
    - 8010:8010
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
  documents-service:
    container_name: bcm-documents-service
    profiles:
    - dev
    - prod
    - platform
    build:
      context: ../platform-services/documents-service
      dockerfile: Dockerfile.auto
    ports:
    - 8010:8010
    environment: []
    networks:
    - bcm-network
    depends_on:
      workflow-intelligence:
        condition: service_healthy
    healthcheck:
      test: curl -f http://localhost:8010/health || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 0G
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
    labels:
      prometheus.scrape: 'true'
      prometheus.port: '8010'
      prometheus.path: /metrics
      service.category: platform
      service.type: application
networks:
  bcm-network:
    driver: bridge
    name: bcm-network
    ipam:
      config:
      - subnet: 172.20.0.0/16
  monitoring-network:
    driver: bridge
    name: monitoring-network
volumes: {}
